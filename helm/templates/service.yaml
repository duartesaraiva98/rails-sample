apiVersion: v1
kind: Service
metadata:
  name: rails-sample
  namespace: {{ .Release.Namespace }}
  labels:
    app: rails-sample
spec:
  selector:
    app: rails-sample
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP